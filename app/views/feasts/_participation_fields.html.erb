<br />
<%= link_to_function "remove","remove_fields(this)", :class => "cl_both" %>	
<%= f.fields_for(:participations, Participation.new, child_index: "new_par", class: "fields") do |fp| %>
		<%=fp.hidden_field(:user_id)%> 
		<%=fp.hidden_field(:_destroy,value: false)%>
		<% obfields = render(partial: 'obligation_fields', locals: {fp: fp})%>
		<div data-obcontent='<%=obfields%>' >
			
		</div>
		
					
<%end%>
	
	